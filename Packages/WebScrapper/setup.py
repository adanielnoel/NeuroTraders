from distutils.core import setup

setup(name='WebScrapper',
      version='0.0',
      description='Web scrapping engine for stock news',
      author='NeuroTraders',
      # author_email='---',
      # url='---',
      install_requires=['sqlalchemy'],  # Used by pip to install dependencies
      python_requires='>=3',
      )
